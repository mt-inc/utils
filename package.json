{
  "name": "@mt-inc/utils",
  "version": "1.0.0",
  "description": "Utils for MT Inc. Project",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "type": "module",
  "author": "Maksym Shamko",
  "license": "MIT",
  "private": true,
  "files": [
    "/dist"
  ],
  "scripts": {
    "tsc": "tsc -p tsconfig.json && tsc -p tsconfig-cjs.json",
    "push": "rm -rf ./dist && yarn tsc && git add . && git commit -m 'working' && git push origin master",
    "pull": "git pull origin master"
  },
  "devDependencies": {
    "@types/node": "^16.10.3",
    "prettier": "^2.4.1",
    "ts-node": "^10.2.1",
    "tsc": "^2.0.3",
    "typescript": "^4.4.2"
  }
}
